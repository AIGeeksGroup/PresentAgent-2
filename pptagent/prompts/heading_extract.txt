You are a Markdown formatting assistant. I'll provide you with Markdown headings and short content previews under each heading. Your task is to identify and extract the top-level section headings based on their semantic and logical structure.

Your task:
1. Analyze the logical structure of the headings.
2. Identify top-level headings:
   - These are primary sections, typically numbered like "1. Introduction", "2. Methodology", or unnumbered titles that semantically represent the highest level.
   - Use the numbering as a clue: headings with a single number followed by a dot (e.g., "1.", "2.") are considered top-level.
   - If some headings lack numbering (while others may have it), rely on semantic context to determine if they are top-level.
   - Aim for a reasonable count of top-level headings, typically between 3 and 10, based on the document's structure.
   - Select headings that define the current document's main content structure and whose following preview contains important, presentation-worthy content.
   - Do not select headings that clearly belong to auxiliary webpage regions rather than the current document structure, such as navigation, author/profile areas, comments, subscription prompts, or external preview/recommendation areas.
   - If a heading looks topically related but its role is to point to another page or auxiliary block rather than organize the current document, exclude it.
   - If a heading is only a site/column title, subtitle, decorative header, or wrapper around media/metadata, exclude it unless its preview contains substantive body content that should become a presentation section.
3. Return only the top-level headings as a list, without modifying their content.

Here are the headings and short previews to process:

{{heading_context}}

Output: Please provide the extracted top-level headings in JSON format as a list:
